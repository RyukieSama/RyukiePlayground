# 二叉树遍历与特殊二叉树

## 一、二叉树的基本概念

二叉树是一种树形结构，每个节点最多有两个子节点：

- 左子节点 `left`
- 右子节点 `right`

下面这棵树作为后续示例：

```text
        1
       / \
      2   3
     / \   \
    4   5   6
```

## 二、深度优先遍历 DFS

深度优先遍历会沿着一条路径尽可能深入，走到尽头后再返回上一层。二叉树中最常见的 DFS 有三种，区别在于根节点的访问时机。

### 1. 前序遍历

顺序：

```text
根节点 → 左子树 → 右子树
```

示例结果：

```text
[1, 2, 4, 5, 3, 6]
```

递归模板：

```swift
func preorder(_ node: TreeNode?, _ result: inout [Int]) {
    guard let node else { return }
    result.append(node.val)
    preorder(node.left, &result)
    preorder(node.right, &result)
}
```

常见用途：复制树、序列化树、先处理父节点再处理子节点的问题。

### 2. 中序遍历

顺序：

```text
左子树 → 根节点 → 右子树
```

示例结果：

```text
[4, 2, 5, 1, 3, 6]
```

二叉搜索树的中序遍历结果一定是升序，这是中序遍历最重要的性质。

递归模板：

```swift
func inorder(_ node: TreeNode?, _ result: inout [Int]) {
    guard let node else { return }
    inorder(node.left, &result)
    result.append(node.val)
    inorder(node.right, &result)
}
```

常见用途：验证二叉搜索树、获取二叉搜索树的第 `k` 小节点。

### 3. 后序遍历

顺序：

```text
左子树 → 右子树 → 根节点
```

示例结果：

```text
[4, 5, 2, 6, 3, 1]
```

递归模板：

```swift
func postorder(_ node: TreeNode?, _ result: inout [Int]) {
    guard let node else { return }
    postorder(node.left, &result)
    postorder(node.right, &result)
    result.append(node.val)
}
```

常见用途：删除树、计算目录大小、计算节点高度、从子节点结果推导父节点结果。

### 4. DFS 的复杂度

设二叉树有 `n` 个节点，高度为 `h`：

- 时间复杂度：`O(n)`，每个节点访问一次。
- 递归空间复杂度：`O(h)`。
- 平衡树中 `h` 约为 `O(log n)`，退化树中 `h` 可能为 `O(n)`。

## 三、广度优先遍历 BFS

BFS（Breadth-First Search）也叫层序遍历，使用队列，按照从上到下、同一层从左到右的顺序访问节点。

示例结果：

```text
[[1], [2, 3], [4, 5, 6]]
```

Swift 模板：

```swift
func levelOrder(_ root: TreeNode?) -> [[Int]] {
    guard let root else { return [] }
    
    var result: [[Int]] = []
    var queue: [TreeNode] = [root]
    var index = 0
    
    while index < queue.count {
        let levelEnd = queue.count
        var level: [Int] = []
        
        while index < levelEnd {
            let node = queue[index]
            index += 1
            level.append(node.val)
            
            if let left = node.left {
                queue.append(left)
            }
            if let right = node.right {
                queue.append(right)
            }
        }
        
        result.append(level)
    }
    
    return result
}
```

使用数组模拟队列时，推荐用 `index` 指向队首，不建议频繁调用 `removeFirst()`，因为后者可能移动剩余元素。

复杂度：

- 时间复杂度：`O(n)`。
- 空间复杂度：`O(n)`，最坏情况下队列会保存一整层节点。

## 四、Morris 遍历

Morris 遍历通过临时修改和恢复树中的指针，在不使用递归栈或显式栈的情况下完成遍历。

特点：

- 时间复杂度：`O(n)`。
- 额外空间复杂度：`O(1)`。
- 实现复杂，必须确保遍历结束后恢复原树结构。

在普通面试题中，递归或显式栈通常更容易理解和维护。

## 五、常见特殊二叉树

### 1. 满二叉树

每个节点要么没有子节点，要么恰好有两个子节点，不存在只有一个子节点的情况。

```text
      1
     / \
    2   3
   / \ / \
  4  5 6  7
```

### 2. 完美二叉树

所有非叶子节点都有两个子节点，并且所有叶子节点位于同一层。

高度为 `h` 的完美二叉树共有：

```text
2^(h + 1) - 1
```

个节点。

完美二叉树一定是满二叉树，但实际语境中“满”和“完美”有时会混用，阅读题目时要以题目定义为准。

### 3. 完全二叉树

除了最后一层外，其他层都被填满；最后一层的节点集中在左侧。

```text
      1
     / \
    2   3
   / \ /
  4  5 6
```

完全二叉树适合使用数组存储：

- 下标为 `i` 的节点左子节点通常是 `2i + 1`。
- 右子节点通常是 `2i + 2`。
- 父节点通常是 `(i - 1) / 2`。

堆通常建立在完全二叉树的基础上。

### 4. 二叉搜索树 BST

对于每个节点：

```text
左子树所有节点值 < 当前节点值
右子树所有节点值 > 当前节点值
```

中序遍历 BST 会得到升序序列。

平均情况下查找、插入和删除为 `O(log n)`，但如果树退化成链表，最坏会变成 `O(n)`。

### 5. 平衡二叉树

平衡二叉树要求左右子树高度差保持在一定范围内，目的是避免树退化。

常见类型：

- AVL 树：严格平衡，左右子树高度差通常不超过 `1`。
- 红黑树：近似平衡，通过颜色规则保证树高为 `O(log n)`。

平衡二叉搜索树的查找、插入和删除通常都能保持 `O(log n)`。

### 6. 堆

堆是一种满足特定顺序关系的完全二叉树。

小根堆：

```text
父节点 <= 子节点
```

大根堆：

```text
父节点 >= 子节点
```

堆不要求整体有序，只保证父节点和子节点之间的关系。获取最小值或最大值通常是 `O(1)`，插入和删除堆顶通常是 `O(log n)`。

### 7. 退化二叉树

如果每个节点都只有一个子节点，二叉树会退化成链表：

```text
1
 \
  2
   \
    3
```

此时树高为 `n`，递归可能产生较深的调用栈，搜索效率也会从 `O(log n)` 退化为 `O(n)`。

## 六、快速判断方法

| 题目关键词 | 优先考虑 |
|---|---|
| 根、左、右 | 前序遍历 |
| 二叉搜索树升序、第 k 小 | 中序遍历 |
| 子树结果、删除节点、计算高度 | 后序遍历 |
| 从上到下、按层、同层 | BFS 层序遍历 |
| 镜像、对称 | 两棵子树交叉递归比较 |
| 最小值/最大值、优先级 | 堆 |
| 有序查找且要求 `O(log n)` | BST 或平衡 BST |

